# Task Feed activity — Starter

Use for **Lecture 23, Slide 49**.

## Your task

Explore the existing `TaskFeed` component, then complete the unfinished Effect so the component can load its tasks from the provided API.

Focus only on the TODOs in `src/TaskFeed.tsx`.

The surrounding state and rendering are already prepared.

## Run

```bash
npm install
npm run dev
```
