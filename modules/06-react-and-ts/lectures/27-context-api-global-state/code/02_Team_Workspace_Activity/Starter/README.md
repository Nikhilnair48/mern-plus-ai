# Team Workspace Activity — Starter

Starter project for Slides 40–42.

The UI is complete and the shared presence-status feature already works through props.
Learners refactor the dependency path to typed Context.

## Run

```bash
npm install
npm run typecheck
npm run dev
```
